"""graph_rag — locally-run Graph RAG pipeline package.

Slice V1 (walking skeleton) establishes the foundation contract: the six
external-dependency ports (ADR-0010), their in-memory fakes, the shared data
models, deterministic document IDs (ADR-0001), config, and the logging seam.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
