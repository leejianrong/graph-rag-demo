"""Real adapters that wrap live services behind the ports of ``graph_rag.ports``.

Populated by later work: MinIO ``ObjectStore``, Elasticsearch ``DocumentStore`` /
``EntityStore``, Neo4j ``GraphStore``, LiteLLM ``LLMClient``, sentence-transformer
``Embedder``. This package exists so those modules are only *added* here.
"""
